---
title: "第4章 数据库安全性"
date: 2026-08-08 11:03:00
tags: [数据库]
categories: [数据管理基础]
description: "第4章 数据库安全性"
---

<!DOCTYPE html>
<html lang="zh-CN">
<head>
<meta charset="UTF-8">
<title>对象与操作类型</title>
<style>
    table {
        border-collapse: collapse;
        width: 100%;
        max-width: 900px;
        margin: 20px auto;
        /* 使用宋体/楷体以贴合原图的中文字体风格 */
        font-family: "KaiTi", "楷体", "SimSun", serif; 
        text-align: center;
        font-size: 20px;
    }
    th, td {
        border: 2px solid #000;
        padding: 12px 16px;
    }
    /* 表头背景色 */
    th {
        background-color: #fff6da; /* 淡黄色 */
        font-weight: bold;
        letter-spacing: 2px;
    }
    /* 蓝色字体样式 (中文) */
    .text-blue {
        color: #0000cd;
        font-weight: bold;
    }
    /* 蓝色字体样式 (英文) */
    .text-blue-en {
        color: #0000cd;
        font-weight: bold;
        font-family: sans-serif;
        letter-spacing: 1px;
    }
    /* 红色字体样式 (英文) */
    .text-red-en {
        color: #c00000;
        font-weight: bold;
        font-family: sans-serif;
        line-height: 1.5;
        letter-spacing: 1px;
    }
</style>
</head>
<body>

<table>
    <thead>
        <tr>
            <th width="15%">对象类型</th>
            <th width="25%">对象</th>
            <th width="60%">操作类型</th>
        </tr>
    </thead>
    <tbody>
        <tr>
            <td rowspan="4" class="text-blue">数据库<br>模式</td>
            <td class="text-blue">模式</td>
            <td class="text-blue-en">CREATE SCHEMA</td>
        </tr>
        <tr>
            <td class="text-blue">基本表</td>
            <td class="text-blue-en">CREATE TABLE, ALTER TABLE</td>
        </tr>
        <tr>
            <td class="text-blue">视图</td>
            <td class="text-blue-en">CREATE VIEW</td>
        </tr>
        <tr>
            <td class="text-blue">索引</td>
            <td class="text-blue-en">CREATE INDEX</td>
        </tr>
        <tr>
            <td rowspan="2" class="text-blue">数据</td>
            <td class="text-blue">基本表和视图</td>
            <td class="text-red-en">
                SELECT, INSERT, UPDATE, DELETE,<br>
                REFERENCES,<br>
                ALL PRIVILEGES
            </td>
        </tr>
        <tr>
            <td class="text-blue">属性列</td>
            <td class="text-red-en">
                SELECT, INSERT, UPDATE,<br>
                REFERENCES,<br>
                ALL PRIVILEGES
            </td>
        </tr>
    </tbody>
</table>

</body>
</html>

## 1 GRANT

```sql
GRANT <权限> [, <权限> ] ... 
ON <对象类型> <对象名> [, <对象类型> <对象名> ] ...
TO <用户> [ , <用户> ] ... 
[ WITH GRANT OPTION ];
```

- `WITH GRANT OPTION`：被授权者可继续将自己接受到的权限授权给其他用户

## 2 REVOKE

```sql
REVOKE <权限> [, <权限>] ... 
ON <对象名> [, <对象名>] ...
FROM <用户> [, <用户>] ... [CASCADE | RESTRICT];
```

- `CASCADE` 表示要进行权限的 ‘连锁回收’
- `RESTRICT` 表示：只有当不存在连锁回收问题时，才能从指定用户那里回收权限

## 3 CREATE USER

```sql
-- CREATE USER <username> [WITH] [DBA | RESOURCE | CONNECT]; 实际上各个数据库相差甚远

CREATE USER username IDENTIFIED BY ‘password’;
```

<!DOCTYPE html>
<html lang="zh-CN">
<head>
<meta charset="UTF-8">
<title>数据库角色与权限</title>
<style>
    table {
        border-collapse: collapse;
        width: 100%;
        max-width: 900px;
        margin: 20px auto;
        text-align: center;
        /* 全局默认使用楷体以贴合原图中文字体 */
        font-family: "KaiTi", "楷体", "SimSun", serif; 
        font-size: 22px;
    }
    th, td {
        border: 2px solid #000;
        padding: 15px 10px;
    }
    /* 表头样式及背景色 */
    th {
        background-color: #fdf3d0; /* 淡黄色 */
        font-weight: bold;
    }
    /* 表头中的英文字体特定样式 */
    .th-en {
        font-family: Arial, sans-serif;
        font-weight: bold;
    }
    /* 第一列角色名称样式 (深蓝色，无衬线粗体) */
    .role {
        color: #0000a0;
        font-family: Arial, sans-serif;
        font-weight: bold;
    }
    /* "可以" 的字体样式 (深蓝色) */
    .yes {
        color: #0000a0;
        font-weight: bold;
        letter-spacing: 1px;
    }
    /* "不可以" 的字体样式 (暗红色) */
    .no {
        color: #b30000;
        font-weight: bold;
        letter-spacing: 1px;
    }
</style>
</head>
<body>

<table>
    <thead>
        <tr>
            <th rowspan="2" width="18%">拥有的权限</th>
            <th colspan="4">可否执行的操作</th>
        </tr>
        <tr>
            <th class="th-en" width="16%">CREATE<br>USER</th>
            <th class="th-en" width="18%">CREATE<br>SCHEMA</th>
            <th class="th-en" width="16%">CREATE<br>TABLE</th>
            <th width="32%">登录数据库，执行<br>数据查询和操纵</th>
        </tr>
    </thead>
    <tbody>
        <tr>
            <td class="role">DBA</td>
            <td class="yes">可以</td>
            <td class="yes">可以</td>
            <td class="yes">可以</td>
            <td class="yes">可以</td>
        </tr>
        <tr>
            <td class="role">RESOURCE</td>
            <td class="no">不可以</td>
            <td class="no">不可以</td>
            <td class="yes">可以</td>
            <td class="yes">可以</td>
        </tr>
        <tr>
            <td class="role">CONNECT</td>
            <td class="no">不可以</td>
            <td class="no">不可以</td>
            <td class="no">不可以</td>
            <td class="yes">可以，但必须拥有<br>相应权限</td>
        </tr>
    </tbody>
</table>

</body>
</html>

## 4 数据库角色

```sql
CREATE ROLE rolename [, rolename] ...;
```

数据库角色：被命名的一组与数据库操作相关的权限

- 角色是权限的集合 
- 可以为一组具有相同权限的用户创建一个角色
- 简化授权的过程

【例】 通过角色来实现将一组权限授予一个用户

- 首先创建一个角色 `R1`
```sql
CREATE ROLE R1;
```

- 然后使用 ``GRANT`` 语句，使角色 ``R1`` 拥有 ``Student`` 表的 ``SELECT``、``UPDATE``、``INSERT`` 权限
```sql
GRANT SELECT, UPDATE, INSERT ON TABLE Student TO R1;
```

- 将这个角色的授予王平，张明，赵玲。使他们具有角色 ``R1`` 所包含的全部权限：
```sql
GRANT R1 TO 王平, 张明, 赵玲;
```

- 可以一次性通过 ``R1`` 来回收王平的这 `3` 个权限
```sql
REVOKE R1 FROM 王平;
```

## 5 视图机制

先创建视图，再把对视图操作的权限授予用户

## 6 AUDIT（审计）

启用一个专用的审计日志（Audit Log）将用户对数据库的所有操作记录在上面

- `AUDIT` 语句：设置审计功能
- `NOAUDIT` 语句：取消审计功能

```sql
AUDIT ALTER, UPDATE ON SC;
```